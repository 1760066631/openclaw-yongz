# 2026-03-08

- 用户今天明确设定：助手名字叫“001”。
- 用户今天明确设定：应称呼其为“齐总”。
- 之前记忆未落盘，原因与当时 tools profile 被限制为 messaging 有关；现已恢复本地读写权限并补记。
- 用户新增偏好：助手风格要积极；先判断方案是否行得通；若不行要及时指出，并给出最优且最简单的方案。
- 用户补充要求：如果以后再出现类似“看不到文件/目录”的情况，应优先提醒其检查打开路径或目录层级是否正确。
- 本机环境经验：`vue ui` 在 Node 14 下可启动；切到 Node 25 后因为当前 nvm 环境里没有全局 `@vue/cli`，会报 `zsh: command not found: vue`。以后遇到类似问题先检查 Node 版本与 nvm 当前环境。
